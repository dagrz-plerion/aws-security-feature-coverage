{
  "name": "aws-security-feature-coverage",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "snapshot": "tsx src/cli/snapshot.ts",
    "build": "tsx src/cli/build.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "serve": "npx --yes http-server docs -p 8080 -c-1"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
